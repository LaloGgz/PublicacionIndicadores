@model IEnumerable<Indicadores.Models.PostgresSql.Tablas.PermisoModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GrupoModel.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecursoModel.url)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GrupoModel.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecursoModel.url)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idpermiso }) |
            @Html.ActionLink("Details", "Details", new { id=item.idpermiso }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idpermiso })
        </td>
    </tr>
}

</table>
